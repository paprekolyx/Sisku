# SISKU · черновой макет интернет-магазина

> **v0.9.0-draft · 20.09.2026.** Макет для оценки дизайна и функций владельцем
> магазина. Название Sisku — временное заглушечное, будет заменено.
> Не является боевым сайтом: юридические страницы, оплата и защита админки —
> следующих версий (см. SECURITY.md).

Мультибрендовый бутик премиальной одежды и парфюмерии: витрина с каталогом,
корзиной и формой заказа + админ-панель (заказы, статистика, сборка, магазин,
управление, пользователи) с мок-авторизацией. Дизайн — «тихая роскошь»:
светлая тема Ivoire (основная) и тёмная Noir & Champagne, обе с переключателями,
контрасты WCAG 2.1 AA.

**Сайт-черновик:** https://paprekolyx.github.io/Sisku/
**Админ-панель:** `…/admin.html` (вход: пароль 123456, указан на странице входа)
**По работе сайта писать:** Albert23NO@ya.ru — ведущий аналитик проекта

---

## Страницы

| Страница | Состав |
|---|---|
| `index.html` | витрина: о магазине, товары (фильтры категория/бренд/наличие/сортировка, карточка с вариантами, корзина с «Очистить», форма заказа с валидацией телефона/почты и промокодом), условия заказа с таблицей размеров в модалке, контакты с кнопкой отслеживания заказа, подвал с брендбуком |
| `login.html` | вход в админ-панель (мок-авторизация: пароль 123456 указан на странице, сессия — флаг вкладки) |
| `admin.html` | вкладка «Заказы → Список заказов»: список с сортировкой, фильтрами, поиском, пагинацией по 30, карточка заказа со сменой статусов по модели переходов и признаком оплаты (отменённые заблокированы), маскирование контактов, CSV; вкладка «Статистика»: KPI, заявки по дням, воронка с метриками времени, топ товаров, доставка/оплата (диаграммы или таблицы), выгрузка статистики в CSV |
| `assembly.html` | «Заказы → Сборка»: обезличенный производственный список заказов в статусе «Сборка» с позициями, остатками склада (сигнализация < 3 шт) и переводом в «Отправлен» |
| `shop.html` | «Магазин → Промокоды»: создание/правка/включение промокодов; скидки проверяются и применяются на сервере |
| `categories.html` | «Магазин → Категории»: CRUD категорий с авто-slug и счётчиком товаров |
| `brands.html` | «Магазин → Бренды»: CRUD брендов, активация, счётчик товаров |
| `products.html` | «Магазин → Товары»: список с активацией, карточка товара с живым предпросмотром витрины, редактор вариантов, черновая загрузка фото (сжатие в браузере), импорт CSV и шаблон |
| `manage.html` | «Управление → Оплата и доставка»: справочники способов с ценами, вилками и активностью |
| `sitecontent.html` | «Управление → Сайт»: правка текстов витрины (site_content) с поиском; сайт подхватывает после Ctrl + F5 |
| `sitemap.html` | «Управление → Карта сайта»: кликабельная схема всех страниц и переходов |
| `users.html` | «Пользователи»: список администраторов, создание/правка кликом по ФИО, удаление, роли: администратор/сборщик/менеджер/бизнес-партнёр |

Модальные окна витрины: корзина и оформление заказа, карточка товара, брендбук
(из подвала), таблица размеров, отслеживание заказа по номеру и коду.

## Стек

| Слой | Технология |
|---|---|
| Вёрстка | HTML5, CSS3 (дизайн-токены на CSS-переменных), vanilla JS без сборки |
| Шрифты | Prata + Manrope, локально (woff2, кириллица), без CDN |
| Компоненты | `ui.js`/`ui.css` — кастомные селекты, датапикеры, степперы ±, автозаполнение в цветах темы |
| Графики | Chart.js (локально, `assets/vendor/`) |
| База | Supabase (PostgreSQL + RLS + серверные функции) — **только для черновика**; боевая версия поедет на российском решении |
| Хостинг | GitHub Pages (Deploy from a branch) |

## Структура

```
index.html  login.html
admin.html  assembly.html  shop.html  categories.html  brands.html  products.html
manage.html  sitecontent.html  sitemap.html  users.html
README.md  LICENSE.md  SECURITY.md
assets/css/      fonts.css · styles.css (витрина) · admin.css · ui.css (общие компоненты)
assets/js/       config.js (КЛЮЧИ БАЗЫ — только здесь, SITE_VERSION) · ui.js · site.js ·
                 admin.js · mockauth.js (гейт входа) · admintheme.js · assembly.js ·
                 shop.js · categories.js · brands.js · products.js · manage.js ·
                 sitecontent.js · users.js
assets/vendor/   supabase.min.js · chart.umd.min.js
assets/fonts/    prata-*.woff2 · manrope-*.woff2
assets/img/      hero.jpg (светлая тема) · hero-dark.jpg (тёмная тема) · products/p001…p008.jpg
data/            brands.csv · categories.csv · products.csv · variants.csv
supabase/        01_schema → 12_manage_policies (выполнять по порядку, все идемпотентны)
docs/            setup-repo-pages.md · setup-supabase.md · feature-proposals.md ·
                 code-review-v040.md · update-v040.md … update-v090.md (инструкции обновлений)
tests/           check-repo.py — автоматическая проверка целостности репозитория
```

## Инструкции

- **`docs/setup-repo-pages.md`** — репозиторий, загрузка файлов, включение Pages.
- **`docs/setup-supabase.md`** — вторая база в том же аккаунте Supabase,
  скрипты 01–04, импорт CSV (или скрипт 05 вместо CSV), куда вставить ключи.
- **`docs/update-v090.md`** — последняя инструкция обновления; далее каждое
  обновление получает свой файл `docs/update-v0XX.md` с чек-листом приёмки.
- Скрипты базы: 05 — запасной посев каталога без CSV; 06 — статус «Оплачен»
  вынесен в признак; 07 — пользователи и защита отменённых; 08 — текстовые правки;
  09 — запросы-сборки против медленной загрузки; 10 — промокоды и отслеживание;
  11 — запись каталога из админки; 12 — справочники и тексты из «Управления».

## Как править

- **Тексты сайта** — админка: «Управление → Сайт» (или Table Editor → `site_content`);
  витрина подхватывает после Ctrl + F5 без публикации кода.
- **Товары** — админка: «Магазин → Товары» (карточка или CSV); категории и бренды —
  соседние подвкладки.
- **Ключи базы** — только `assets/js/config.js`; там же константа версии.
- После публикации — обновить сайт с **Ctrl + F5**.

## Из учебного проекта сохранены сильные решения

- Заказ создаётся только серверной функцией `create_order()`: цены и остатки
  из базы, подмена суммы из браузера невозможна; одна транзакция; снапшоты
  цены, названия и варианта в позициях; анти-спам 3 заказа / 10 минут; honeypot.
- Статусная модель Jira-workflow (`status_transitions`) + журнал истории;
  смена статуса только через `admin_set_status()` с проверкой переходов.
- **Оплата — признак `is_paid` + дата, а не статус** (статус «Оплачен» удалён
  из модели в v0.2.0).
- Промокоды проверяются сервером (`check_promo`), скидка пересчитывается
  в `create_order()` — публичного чтения кодов нет.
- Тексты в `site_content` правятся без кода; корзина в localStorage; скелетоны;
  экранирование вывода; блокировка повторной отправки форм.
- Маскирование контактов в админке + раскрытие кнопкой; CSV с BOM и `;`.
- Идемпотентные нумерованные SQL-скрипты; версия на всех страницах.

## Осознанные упрощения черновика

- Мок-авторизация: пароль 123456 живёт на странице входа и в JS, не в базе;
  нет ролевых ограничений страниц, сессий, 2FA, аудита (см. SECURITY.md).
- Запись каталога и справочников открыта анонимно (draft-политики 11–12) —
  для демонстрации CRUD без серверного API.
- Нет политики ПДн, оферты, согласий, уведомлений — появятся в боевой версии.
- Фото товаров — сгенерированные заглушки; черновик фото хранит dataURL-ом
  в записи товара, боевой вариант — WebP в российском хранилище.
- База Supabase вне России: **только тестовые данные**, реальные ПДн не вводим.

## Версионирование

SemVer: MAJOR — несовместимая архитектура, MINOR — волна функциональности,
PATCH — правки. Черновик живёт в 0.x. Таблица — в хронологическом порядке:

| Версия | Что вошло |
|---|---|
| 0.1.0-draft | витрина, корзина, заказ через `create_order()`, админка: заказы + статистика |
| 0.2.0-draft | кастомные селекты, тема в админке, «На сайт», новая карточка заказа, «Очистить» корзину, autofill в цветах темы, брендбук, ссылка на репозиторий, Telegram вместо WhatsApp, статус «Оплачен» вынесен из модели в признак, доставка/оплата в статистике разнесены (диаграммы/таблицы), метрики времени статусов |
| 0.3.0-draft | мобильная админка: адаптив топбара/тулбара/карточки заказа, таблица заказов на телефоне показывает 4 столбца (№, Клиент, Сумма, Статус); десктоп не изменён |
| 0.4.0-draft | экран входа (мок-пароль 123456), страницы «Сборка» и «Пользователи» с черновой ролевой моделью, сортировка таблицы заказов, блокировка изменений отменённых заказов, валидация телефона/почты по российским маскам, 4-я метрика времени и «кол-во» в статистике, топ товаров таблицей, фиксированная толщина столбцов, период «всё время» по умолчанию, три способа оплаты в условиях, брендбук: карточки типографики и ровные плашки палитры, скроллбары в стиле сайта, документ предложений docs/feature-proposals.md |
| 0.5.0-draft | hero зависит от темы (светлая — женский образ, тёмная — мужской), вкладки админки без подчеркиваний, админка/статистика/сборка грузятся одним запросом-сборкой (draft_admin_bundle, draft_assembly_bundle), на «Сборке» перевод заказа в «Отправлен» и сигнализация остатка < 3 шт, редактирование админа кликом по ФИО (без жирного), удаление администраторов, защита от повторных кликов «Сохранить» |
| 0.6.0-draft | промокоды (серверные check_promo/create_order v2, раздел «Магазин»), отслеживание заказа покупателем по номеру + коду, выгрузка статистики в CSV со всеми показателями, хронология графика по дням, таблица по умолчанию в «Доставка и оплата» без прыжков верстки, тонкие столбцы диаграмм, яркая пилюля статуса, подсветка группы строк на «Сборке», таблица размеров XS–XL в «Условиях заказа» |
| 0.7.0-draft | фирменные датапикеры и степперы ± вместо нативных, «Использовано N из M лимита» и редактирование промокода кликом по коду, пагинация заказов по 30, фикс даты по локальному времени в графике и CSV (убран фантомный день), тонкая воронка, таблица размеров в модалке, кнопка отслеживания как акцентная, равные плашки палитры, чистый подвал |
| 0.8.0-draft | управление каталогом из админки: раздел «Товары» (список, активация, карточка с живым предпросмотром витрины, редактор вариантов, черновая загрузка фото со сжатием в браузере, импорт CSV и шаблон), CRUD категорий в «Магазине», переключатель «Промокоды и категории / Товары» |
| 0.9.0-draft | новая навигация админки: Заказы (подвкладки «Список заказов» и «Сборка»), Магазин (Промокоды / Категории / Бренды / Товары — категории отдельной страницей), новая вкладка «Управление» (Оплата и доставка / Сайт / Карта сайта); CRUD брендов, справочников оплаты-доставки и текстов site_content из админки; кликабельная карта сайта; тема применяется до отрисовки — «моргание» устранено; версия на всех страницах включая вход |

## Контакты

- **По работе сайта (ошибки, правки, вопросы макета):** Albert23NO@ya.ru —
  ведущий аналитик проекта.
- Черновые значения на сайте (телефон, e-mail, Telegram) — заглушки в `site_content`.
- Боевые контакты владельца появятся в контент-пакете волны M0 (см. migration-plan.md).
